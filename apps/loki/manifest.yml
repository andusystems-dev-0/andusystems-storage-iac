# apps/loki/manifest.yml
# MinIO credentials for Loki — override via Ansible Vault
apiVersion: v1
kind: Secret
metadata:
  name: loki-minio-credentials
  namespace: loki
type: Opaque
stringData:
  rootUser: "{{ minio_root_user }}"
  rootPassword: "{{ minio_root_password }}"
