# apps/tempo/manifest.yml
# MinIO credentials for Tempo — override via Ansible Vault
apiVersion: v1
kind: Secret
metadata:
  name: tempo-minio-credentials
  namespace: tempo
type: Opaque
stringData:
  rootUser: "{{ minio_root_user }}"
  rootPassword: "{{ minio_root_password }}"
